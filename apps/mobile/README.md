# Mobile application

Reserved for the React Native Android/iOS application in Phase 1.

The mobile shell starts only after the authentication/tenant context contract and standalone API boundary are stable.
